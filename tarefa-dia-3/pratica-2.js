let contador = 1;
let numTabuada = 5;

console.log("Tabuada do número" + numTabuada);


while (contador <= 10) {
    console.log(numTabuada + "x" + contador + "=", (numTabuada * contador));
    contador ++
}